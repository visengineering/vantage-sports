{
  "description": "These are only utils. Please refer to standalone client and backend package.jsons",
  "scripts": {
    "start": "concurrently \"cd backend/api && yarn start\" \"cd client && yarn start\"",
    "prettier:check": "prettier --check .",
    "prettier:fix": "prettier --write .",
    "lint": "yarn prettier:check"
  },
  "dependencies": {
    "concurrently": "^7.0.0",
    "prettier": "^2.6.0"
  },
  "name": "vantage-repo",
  "version": "0.0.0",
  "license": "UNLICENSED",
  "private": true
}
